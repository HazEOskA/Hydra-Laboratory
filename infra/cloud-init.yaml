#cloud-config
package_update: true
package_upgrade: true
packages:
  - bash
  - binutils
  - ca-certificates
  - curl
  - docker.io
  - git
  - python3
  - python3-venv
  - tar
  - zstd

runcmd:
  - [systemctl, enable, --now, docker]

final_message: "Hydra Hermes host baseline complete. Install approved Node.js >=22.19, reconnect, then run remote-preflight.sh."
